{
  "name": "netlify-func-hide-key",
  "version": "1.0.0",
  "description": "A solution to show how to hide api using Netlify functions",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "netlify": "./node_modules/.bin/netlify",
    "debug": "netlify dev --inspect"
  },
  "author": "kamrul",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1"
  },
  "devDependencies": {
    "netlify-cli": "^3.24.3"
  }
}
